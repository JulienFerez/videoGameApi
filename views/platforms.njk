{% extends "layout.njk" %}

{% block content %}
  <div>

    <p>
        {% for platform in platforms %}
        <li><a href=/platform/{{games.id}}>{{ platform.name }}</a></li>
        {% else %}
       <li>This would display if the 'item' collection were empty</li>
        {% endfor %}


      </p>
      </p>
  </div>
{% endblock %}
